{
  "name": "agent-passport-subgraph",
  "version": "1.0.0",
  "private": true,
  "description": "Agent Passport & Reputation Graph subgraph (ENSv2 x World Selfie Check) on Sepolia.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:sepolia": "graph deploy --node http://127.0.0.1:8021/ --ipfs http://127.0.0.1:5001 agent-passport --version-label v0.0.5",
    "test:smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.95.0",
    "@graphprotocol/graph-ts": "^0.36.0"
  }
}